anthropic>=0.40.0
gspread>=6.0.0
google-auth>=2.30.0
feedparser>=6.0.11
requests>=2.32.0
rapidfuzz>=3.9.0
python-dateutil>=2.9.0
# 6.0.2 is the first release with prebuilt wheels for Python 3.12/3.13 built
# against Cython 3. Older versions have no 3.12 wheel, so pip builds them from
# source with whatever Cython is around, and an old Cython produces a broken
# _yaml that raises "Metaclasses with custom tp_new are not supported" on
# import — before any of this code runs.
PyYAML>=6.0.2
ulid-py>=1.1.0
beautifulsoup4>=4.12.0

# lxml is optional and deliberately not required. It is a faster, more
# forgiving HTML parser, but it is a C extension that has to compile when no
# wheel matches your platform, and a failed lxml build aborts the whole
# `pip install -r requirements.txt` — taking every other package down with it.
# drafting.fetch_extract falls back to the stdlib html.parser when lxml is
# absent, and the test suite passes either way. Install it if you want it:
#
#     pip install lxml
# ---- the hosted product ---------------------------------------------------
# The single-tenant install needs none of these: it keeps its rows in a Sheet
# and has no server. They are here rather than in a second requirements file
# because one file that installs cleanly beats two that can disagree, and the
# whole point of this install is that it does not fail halfway.
SQLAlchemy>=2.0.30
# psycopg 3 with the binary wheel: psycopg2 needs libpq and a compiler on the
# machine, which is exactly the class of install failure this project already
# spent a day on.
psycopg[binary]>=3.2
alembic>=1.14
fastapi>=0.115
uvicorn[standard]>=0.32
itsdangerous>=2.2
python-multipart>=0.0.12
cryptography>=43
pydantic>=2.9

pytest>=8.0.0
# TestClient is httpx-based, so the API tests need it even though nothing in
# the product makes an httpx call.
httpx>=0.28
